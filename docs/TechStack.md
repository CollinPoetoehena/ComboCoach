# Technology Stack

TODO: explain here what technology stack used: only frontend since ComboCoach is a training app that generates combinations locally (no database, no user accounts, no external data), so there is no need for user login, etc., it is just boxing logic implemented without the need for a database, etc. This makes it a lightweight and accessible service.